import React from "react"
import { useMenuItems } from "../content-hooks/landing-page/useMenuItems"

const Menu = () => {
  let menuItems = useMenuItems('Landing Page Fast Menu')
  return (
    <div >
      colo
    </div>
  )
}

export default Menu
